# Google OAuth (SPA, PKCE). Crear en Google Cloud Console:
# APIs & Services → Credentials → Create OAuth 2.0 Client ID → Application type: Web application
# Authorized JavaScript origins: http://localhost:5173, https://tu-dominio.com
# Authorized redirect URIs: http://localhost:5173/auth/callback, https://tu-dominio.com/auth/callback
VITE_GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com

# Dropbox OAuth (SPA, PKCE). Crear en https://www.dropbox.com/developers/apps → Scoped access → App folder
# Permisos: files.content.write, files.content.read
# Redirect URI: http://localhost:5173/auth/callback (y tu dominio en producción)
VITE_DROPBOX_CLIENT_ID=tu-dropbox-app-key

# URL del proxy OAuth (Cloudflare Worker). Desarrollo: http://localhost:8787 | Producción: https://oauth-proxy.genmypass.workers.dev
VITE_OAUTH_PROXY_URL=https://oauth-proxy.tudominio.workers.dev
